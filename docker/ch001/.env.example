# 複製成 .env 並填入實際值。.env 不進版控。
CH001_DB_USER=ch001
CH001_DB_PASSWORD=
CH001_DB_NAME=ch001
CH001_DB_PORT=5436

# 給同事的唯讀帳號密碼（供 scripts/ch001_readonly.sql 使用）
CH001_READONLY_PASSWORD=

# 查核頁的登入帳密（HTTP Basic），給會計／採購／業務用
CH001_VIEWER_USER=ct
CH001_VIEWER_PASSWORD=
CH001_VIEWER_PORT=8092
